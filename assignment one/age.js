"use strict";
const prompt = require("prompt-sync")();
while (true) {
  
const age = prompt("How old are you?....");
  console.log(age);
  if (age >= 18) {
    console.log("accepted");
    
  }
 
  break;
}

const prompt = require("prompt-sync")();

do {if (age >= 18) {
  console.log("accepted");
  
}
   
   } while (age <=18);
